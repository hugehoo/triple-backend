server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://0.0.0.0:3306/triple
    username: user
    password: password

  sql:
    init:
      schema-locations: classpath*:scheme.sql
      mode: always
